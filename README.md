# Mini-complier-
The Mini Compiler is a C-based program that converts infix expressions into postfix notation and evaluates them. It uses stack data structures to simulate basic compiler functionality. This project demonstrates core concepts of expression parsing, operator precedence, and stack-based evaluation.
